/**
 * This file contains TypeScript types which every component can use and implement,
 * to contain a stable API trough every component.
 */

/**
 * Type which indicates, which type of sizes a component can use.
 */
export type FDComponentSize = "small" | "medium" | "large";
